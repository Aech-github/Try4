function setup() {
  createCanvas(600, 600);
  background(300,150,0);
  strokeWeight(10);
  stroke('white');
 

}

function draw() {
 // declare variables 
  let posX = mouseX;
  let PosY = mouseY;
  // place a dot on the screen
   point( posX,PosY);
  
}